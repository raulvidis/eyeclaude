__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Cross-agent symlinks (not tracked in git)
.claude/commands
.cursor/commands
.opencode/commands
# But .agents/ is tracked — it's the canonical source for all cross-agent commands
!/.agents/
# ...except the shared-skills symlink, which is per-machine (recreate via agent-repo-standards/link-skills.sh)
/.agents/skills

