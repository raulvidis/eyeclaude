Register terminal windows with EyeClaude for eye-tracking focus management.

Run the following command to interactively pick which terminal windows to register:

```bash
eyeclaude register --pick
```

This will list all Windows Terminal windows with their screen positions and let you choose which ones to track.

Confirm registration was successful by telling me the output.
